# runtime fall-alert evidence (patient snapshots — never commit)
alerts/
out.mp4

# python
__pycache__/
*.pyc
.venv/
venv/
pipenv/

# auto-generated debug snapshots (hundreds of frames from --snap-every runs)
debugs/

# README image assets (hardware photos, state screenshots) — not committed
devices/
docs/

# web app
node_modules/
.env

# OS
.DS_Store
